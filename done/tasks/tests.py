from django.test import TestCase


from tasks.models import Tasks
# display task number
task_count=1
# display task quantity
task_qty = Tasks.objects.filter(new_task=1).count()
print(task_qty)
# display task name
new_task = Tasks.objects.filter(new_task=1)[0].name
# print(database[1])

# Get the first item from the filtered queryset

if is task:
    ewd
    
elif is project
